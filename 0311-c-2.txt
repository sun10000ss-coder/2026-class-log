// Online C compiler to run C program online
#include <stdio.h>

int main() {
    
    int num;
    
    printf("숫자를 입력하세요: ");
    scanf("%d", &num); 
    if(num % 2 == 0) // if 는 조건문이다 () 소괄호를 써야한다 ; 쓰지 말아야한다
    {printf("짝수\n");
    printf("2로 나눈 나머지가 0\n");} 두줄 이상 나오면 대괄호를 써야한다 {}
    else
    {printf("홀수\n");
    printf("2로 나눈 나머지가 1\n");}
    
    return 0;
}


> 크다
< 작다
>= 이상
<= 이하
== 같다 
?= 같지 않다